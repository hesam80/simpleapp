version: 2.1
jobs:
  build:
    docker:
      - image: python:3.7
    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'

