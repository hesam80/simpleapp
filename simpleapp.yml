kind: ExternalService
name: simpleapp
spec:
  allow_http: true
  image: hesam80/simpleapp:latest
  image_pull_policy: Always
  image_pull_secret: dockerhub
  path: /
  port: 80
  replicas: 1
  resources:
    memory: 200Mi
